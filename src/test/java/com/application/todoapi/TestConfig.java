
package com.application.todoapi;

import org.springframework.boot.test.context.TestConfiguration;

@TestConfiguration
public class TestConfig {
    // PasswordEncoder bean is now provided by SecurityBeans in main config
}
